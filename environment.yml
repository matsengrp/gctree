name: gctree
channels:
  - conda-forge
  - defaults
  - bioconda
dependencies:
  - scons
  - nestly
  - phylip
  - seqmagick
  - pip
  - pip:
    - . # installs gctree package from current directory "."
