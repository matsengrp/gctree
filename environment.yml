name: gctree
channels:
  - conda-forge
  - defaults
  - bioconda
dependencies:
  - python=3.10
  - phylip
  - pip
  - pip:
    - . # installs gctree package from current directory "."
    - -r requirements.txt
